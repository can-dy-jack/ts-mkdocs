import type { Plugin } from '../plugins.js'

/** Social cards meta tags are rendered via base.html using page meta */
export const socialPlugin: Plugin = {
  name: 'social',
}

export const metaPlugin: Plugin = {
  name: 'meta',
}
